# Development dependencies (tests, linting, type checking).
# Install with:  python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
numpy>=1.26
ruff>=0.3
black>=24.0
mypy>=1.9
# Layer-3b graph coach + its hermetic evals (tests guard with importorskip).
langgraph>=0.2
langchain-core>=0.3
